import DesignSystemClient from "./DesignSystemClient";

export const metadata = { title: "Design System — Haust Safe" };

export default function DesignSystemPage() {
  return <DesignSystemClient />;
}
